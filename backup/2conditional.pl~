#!/usr/bin/perl
use warnings;
use strict;

print "Enter a number: ";
chomp (my $num = <STDIN>);

$num =~ /[0-9]+/ 
$num == "2150339" ? print "Its your emp code !!\n":
print "Its a number\n": print "Please enter numbers only\n";
